--- Makefile.orig	2011-02-10 17:33:53.299236006 +0200
+++ Makefile	2011-02-10 17:34:18.751236000 +0200
@@ -5,14 +5,14 @@
 ## Installation directory. By default, go in /usr/local.
 ## Distributions should probably use /, but they probably know better...
 ifndef PREFIX
-  PREFIX = /usr/local
+  PREFIX = /opt/canmore/local
 endif
 
 ## Compiler to use (modify this for cross compile).
-CC = gcc
+CC = i686-cm-linux-gcc
 ## Other tools you need to modify for cross compile (static lib only).
-AR = ar
-RANLIB = ranlib
+AR = i686-cm-linux-ar
+RANLIB = i686-cm-linux-ranlib
 
 ## Uncomment this to build tools using static version of the library.
 ## Mostly useful for embedded platforms without ldd, or to create
